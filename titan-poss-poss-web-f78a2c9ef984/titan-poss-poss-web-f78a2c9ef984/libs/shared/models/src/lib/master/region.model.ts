export interface Region {
  configDetails: {};
  description: string;
  isActive: boolean;
  parentRegionCode: string;
  regionCode: string;
  orgCode: string;
}
export interface RegionSummary {
  regionCode: string;
  description: string;
}
