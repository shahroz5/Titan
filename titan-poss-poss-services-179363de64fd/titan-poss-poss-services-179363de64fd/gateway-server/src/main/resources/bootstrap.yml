spring:
  application:
    name: gateway-server
  cloud:
    config:
      uri: http://config-server/config
      name: gateway-server
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
logging:
    level:
        ROOT: ERROR

