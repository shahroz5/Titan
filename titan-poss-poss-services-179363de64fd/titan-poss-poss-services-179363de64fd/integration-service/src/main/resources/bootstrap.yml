spring:
  application:
    name: integration-service
  profiles:    active: dev
  cloud:
    config:
      uri: http://config-server/config
      name: integration-service
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
      fallbackLocation: fallback/integration

