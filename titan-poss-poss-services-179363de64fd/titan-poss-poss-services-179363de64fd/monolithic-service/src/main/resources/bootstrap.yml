spring:
  application:
    name: monolithic-service
  profiles:    active: dev
  cloud:
    config:
      uri: http://config-server/config
      name: monolithic-service
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
      fallbackLocation: fallback/monolithic