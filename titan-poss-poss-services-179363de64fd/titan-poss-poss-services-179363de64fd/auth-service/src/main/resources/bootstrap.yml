spring:
  application:
    name: auth-service
  profiles:    active: dev
  cloud:
    config:
      uri: http://config-server/config
      name: auth-service
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
      fallbackLocation: fallback/auth
