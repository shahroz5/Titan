spring:
  application:
    name: config-service
  profiles:    active: dev
  cloud:
    config:
      uri: http://config-server/config
      name: config-service
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
      fallbackLocation: fallback/config

