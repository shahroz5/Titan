spring:
  application:
    name: user-service
  profiles:    active: dev
  cloud:
    config:
      uri: http://config-server/config
      name: user-service
      label: v2
      username: configadmin
      password: SrrL28WdGwaXtSRM
      fallbackLocation: fallback/user
